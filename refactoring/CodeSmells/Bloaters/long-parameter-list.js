// NOTE: INITIAL CODE GENERATED BY AI AND REFACTORED BY ME

// Before Refactoring: Long Parameter List
function createOrderBefore(
    customerId,
    productId,
    quantity,
    price,
    discount,
    shippingAddress,
    billingAddress
) {
    // Logic to create an order
    console.log(`Order created for customer ${customerId}`);
}

// Example usage
createOrderBefore(1, 101, 2, 50, 5, "123 Main St", "456 Elm St");

// After Refactoring: Replace Parameter with Method Call
function createOrderAfter(customerId, productDetails) {
    // Logic to create an order
    console.log(`Order created for customer ${customerId}, product details:`, productDetails);
}

function getProducts(productId, quantity, price) {
    return {
        productId,
        quantity,
        price,
    };
}

// Example usage
createOrderAfter(1, getProducts(101, 2, 50));
